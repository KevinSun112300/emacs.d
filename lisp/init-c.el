;;; package --- init c
;;; Commentary:
                                        ; init c and c++ realate package
;;; Code:
(when (maybe-require-package 'cmake-mode)
  )
(provide 'init-c)
;;; init-c.el ends here
